spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:atc_subscription
    password: password
    username: sa
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true